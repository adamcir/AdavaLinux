# Serial Console Install Implementation Plan

> Current worktree status is tracked in [2026-04-02-boot-console-worktree-state.md](/home/adam/Dokumenty/Projekty/Linux/AdavaLinux/docs/plans/2026-04-02-boot-console-worktree-state.md). This plan predates the later console-order change and the initramfs fallback console work.

> **For Claude:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** Make installed AdavaLinux usable in `qemu -nographic` by enabling serial console login and serial kernel output without removing VGA console support.

**Architecture:** Keep the existing `tty1` path for local display and add `ttyS0` in parallel. Update the installed system boot entries generated by `install.sh` so the kernel logs to both `tty1` and `ttyS0`, and update rootfs init scripts so BusyBox init exposes a shell/login on `ttyS0`.

**Tech Stack:** POSIX shell, BusyBox init/inittab, GRUB config generation, shell-script tests.

---

### Task 1: Add failing tests for serial console support

**Files:**
- Create: `tests/install_sh_serial_console_test.sh`
- Create: `tests/rootfs_serial_console_test.sh`

**Step 1: Write the failing tests**
- Assert generated `grub.cfg` lines in `filesforlinux/rootfs/root/install.sh` include both `console=tty1` and `console=ttyS0`.
- Assert `filesforlinux/rootfs/etc/inittab` contains a `ttyS0` respawn entry.
- Assert `filesforlinux/rootfs/etc/init.d/rcS` writes MOTD to `ttyS0`.

**Step 2: Run tests to verify they fail**

Run: `sh tests/install_sh_serial_console_test.sh && sh tests/rootfs_serial_console_test.sh`

Expected: FAIL because serial console support is not yet present.

### Task 2: Implement minimal serial console support

**Files:**
- Modify: `filesforlinux/rootfs/etc/inittab`
- Modify: `filesforlinux/rootfs/etc/init.d/rcS`
- Modify: `filesforlinux/rootfs/root/install.sh`

**Step 1: Add serial getty**
- Add a `ttyS0` respawn line that mirrors the existing `tty1` login behavior.

**Step 2: Add serial MOTD output**
- Extend `rcS` to clear and print MOTD on `/dev/ttyS0` when present.

**Step 3: Add serial kernel console for installed boots**
- Append `console=ttyS0` to each generated installed-system GRUB menuentry while keeping `console=tty1`.

### Task 3: Verify

**Files:**
- Test: `tests/install_sh_serial_console_test.sh`
- Test: `tests/rootfs_serial_console_test.sh`

**Step 1: Run targeted tests**

Run: `sh tests/install_sh_serial_console_test.sh && sh tests/rootfs_serial_console_test.sh`

Expected: PASS

**Step 2: Run existing checks**

Run: `sh -n filesforlinux/rootfs/root/install.sh && sh tests/install_sh_busybox_applets_test.sh && sh tests/install_sh_grub_dir_test.sh && sh tests/install_sh_init_cmdline_test.sh`

Expected: PASS
